SANKET SHINDE
15:06
1. Write a program that takes a number as input and checks whether it is positive, negative, or zero using `if-else`.

2. Write a program that takes a string as input and prints the length of the string.

3. Write a program that prints numbers from 1 to 10 using a `for` loop.

4. Write a program that calculates the sum of the first 5 natural numbers using a `while` loop.

5. Write a program that reads a text file named `data.txt` and prints its content.
6. Write a program that asks the user to enter a number and handles `ValueError` if the input is not a number.

7. Write a program that takes a list of integers and prints the largest and smallest numbers in the list.

8. Write a program that takes a string as input and prints each character in the string using a `for` loop.

9. Write a program that appends `"Hello, Python!"` to a file named `output.txt`.

10. Write a program that takes a number as input and prints its multiplication table from
1 to 10.